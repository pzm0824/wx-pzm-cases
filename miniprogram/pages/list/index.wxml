<!--pages/list/index.wxml-->
<view class='pzm-page1'>
  <view class='title'>Elements 元素</view>
  <navigator url="../myComponentCase/{{item.name}}/index" class="nav-list" wx:for="{{elements}}" wx:key="{{index}}">
    <view class="nav-title">{{item.title}}</view>
    <view class="nav-name">{{item.name}}</view>
    <pzm-icon type='icon-right' class="iconfont"></pzm-icon>
  </navigator>
</view>

<view class='pzm-page1'>
  <view class='title'>实例</view>
  <navigator url="../myComponentCase/{{item.name}}/index" class="nav-list" wx:for="{{cases}}" wx:key="{{index}}">
    <view class="nav-title">{{item.title}}</view>
    <view class="nav-name">{{item.name}}</view>
    <pzm-icon type='icon-right' class="iconfont"></pzm-icon>
  </navigator>
</view>