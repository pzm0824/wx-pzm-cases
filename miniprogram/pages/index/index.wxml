<pzm-card
  wx:for="{{goods_list}}" 
  wx:key="{{index}}" 
  title='{{item.title}}' 
  des='{{item.des}}' 
  img_url="{{item.img_url}}" 
  now_price="{{item.now_price}}" 
  old_price="{{item.old_price}}" 
  bind:click='buy'
  data-base='{{item}}'
  strongT="{{strongT}}">
</pzm-card>